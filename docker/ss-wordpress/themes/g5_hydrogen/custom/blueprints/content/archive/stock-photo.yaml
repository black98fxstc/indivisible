name: Stock Photo
description: Options for displaying stock photo
type: archive

form:
  fields:

    enabled:
      type: input.checkbox
      label: Enabled
      description: Display stock photo on archive type page.
      default: 1

    width:
      type: input.text
      label: Image Width
      description: Image width in pixels.
      default: 225

    height:
      type: input.text
      label: Image Height
      description: Image height in pixels.
      default: 275

    position:
      type: select.select
      label: Image Position
      description: Choose if the image should be aligned to left, right or none.
      default: none
      options:
        left: Left
        right: Right
        none: None
